define service {
    service_description     Redis latency check
    use                     redis-service
    host_name               redis
    register                0
    check_command           check_redis_latency
}
