define service {
    service_description     Redis-connect
    use                     redis-service
    host_name               redis
    register                0
    check_command           check_redis_connect
}
