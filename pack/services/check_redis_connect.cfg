define service {
    service_description     Redis connection check
    use                     redis-service
    register                0
    host_name               generic-redis
    check_command           check_redis_connect
}
